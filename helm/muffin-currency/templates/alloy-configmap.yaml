apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.alloy.filesConfigMap.name }}
  labels:
    {{- include "muffin-currency.labels" . | nindent 4 }}
data:
  {{- range .Values.alloy.filesConfigMap.files }}
  {{ . }}: |-
    {{- $.Files.Get (printf "files/%s" .) | nindent 4 }}
  {{- end }}
